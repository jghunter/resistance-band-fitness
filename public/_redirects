# /bands is a standalone static page (generated by
# resistance_bands_rails/generate_bands_html.rb).
# These rules must stay ABOVE the SPA catch-all — first match wins,
# and per CF docs redirects apply even when a static asset matches.
/bands /bands/index.html 200
/bands/* /bands/index.html 200

# SPA catch-all: everything else serves the app shell
/* /index.html 200
