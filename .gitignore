# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# diagnostic leftover (safe to delete)
_parsecheck.cjs

# Firebase emulator leftovers. The emulator writes logs and, if you ever export
# its data, a dump directory. None of it belongs in the repo; the emulator's
# whole value is that its data is throwaway.
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
.firebase/
emulator-data/
